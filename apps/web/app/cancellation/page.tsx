import PolicyView from "../../components/policy/PolicyView"

export const metadata = { title: "Cancellation Policy — Little Stepz" }

export default function Page() {
  return <PolicyView slug="cancellation" />
}
